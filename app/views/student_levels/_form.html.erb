<%= form_with(model: student_level, local: true) do |form| %>
  <% if student_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_level.errors.count, "error") %> prohibited this student_level from being saved:</h2>

      <ul>
      <% student_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :student_level_user_id %>
  </div>

  <div class="field">
    <%= form.label :level_name %>
    <%= form.text_field :level_name, id: :student_level_level_name %>
  </div>

  <div class="field">
    <%= form.label :generic_text %>
    <%= form.text_field :generic_text, id: :student_level_generic_text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
